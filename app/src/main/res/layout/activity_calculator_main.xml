<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_calculator_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    tools:context="example.codeclan.com.calculatordroid.CalculatorMainActivity">

    <EditText
        android:id="@+id/NumberOneEditText"
        android:hint="@string/EnterNumber"
        android:inputType="number"
        android:textAlignment="center"
        android:textSize="20sp"
        android:textColor="@color/colorPrimary"
        android:layout_marginTop="30dp"
        android:paddingLeft="20dp"
        android:paddingRight="20dp"
        android:layout_width="250dp"
        android:layout_height="wrap_content" />

    <EditText
        android:id="@+id/NumberTwoEditText"
        android:hint="@string/EnterNumber"
        android:inputType="number"
        android:textSize="20sp"
        android:textAlignment="center"
        android:paddingLeft="40dp"
        android:paddingRight="40dp"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        />

    <Button
        android:id="@+id/CalculateBtn"
        android:onClick="onClickAdd"
        android:text="@string/AddButton"
        android:textAllCaps="false"
        android:textSize="20sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        />

    <Button
        android:id="@+id/CalculateSubtract"
        android:onClick="onClickSubtract"
        android:text="@string/SubtractButton"
        android:textAllCaps="false"
        android:textSize="20sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        />

</LinearLayout>
